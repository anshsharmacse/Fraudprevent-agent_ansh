import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/sonner";
import { ThemeProvider } from "@/components/theme/theme-provider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Financial Transaction Risk Agent | Indian Bank Fraud Detection",
  description: "AI-powered fraud detection system for Indian banks using OpenRouter and structured validation. Analyze transactions and detect suspicious activity in real-time.",
  keywords: ["Fraud Detection", "Indian Bank", "AI", "Transaction Analysis", "Risk Assessment", "OpenRouter", "Zod Validation"],
  authors: [{ name: "Financial AI Team" }],
  icons: {
    icon: "/logo.svg",
  },
  openGraph: {
    title: "Financial Transaction Risk Agent",
    description: "AI-powered fraud detection for Indian banks",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
